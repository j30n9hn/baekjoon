#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int parlin() {
//바로 호출 후 값 저장
}
int main() {
	char num[5];
	char rnum[5];
	int* sol = (int*)malloc(sizeof(int) * 1);

	for (int i = 0; ; i++) {
		scanf("%s", num);
		

